import 'package:flutter/cupertino.dart';

class NumberWithText extends StatelessWidget {
  const NumberWithText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
